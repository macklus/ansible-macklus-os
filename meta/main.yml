galaxy_info:
  role_name: os
  author: José Pedro Andrés
  description: Simple role to manage Operating Systems default operations
  company: none
  license: GPL-3.0-only
  min_ansible_version: 2.4
  # https://galaxy.ansible.com/api/v1/platforms/
  #
  platforms:
  - name: Ubuntu
    versions:
    - all
  - name: Debian
    versions:
    - all
  - name: Fedora
    versions:
    - all

  galaxy_tags: [ 'macklus', 'os', 'update', 'packages', 'ubuntu', 'debian', 'ppa', 'fedora', 'centos']

dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.